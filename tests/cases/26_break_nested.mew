# break should exit only the inner loop
seen = []
for i = 1 to 3 do
    for j = 1 to 5 do
        if j == 3 then break end
        push(seen, [i, j])
    end
end
print(len(seen))
for pair in seen do print(pair) end
